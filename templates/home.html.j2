<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}EduGrok{% endblock %}</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="{{ url_for('static', filename='style.css') }}" rel="stylesheet">
    {% block head %}{% endblock %}
</head>
<body class="min-h-screen {{ theme|default('astronaut') }} bg-gray-100 dark:bg-gray-800 text-gray-900 dark:text-white">
    <header class="bg-white dark:bg-gray-700 p-4 shadow">
        <h1 class="text-3xl font-bold">EduGrok</h1>
        <nav class="mt-2">
            <a href="{{ url_for('home') }}" class="mr-4 text-blue-500 hover:underline">Home</a>
            <a href="{{ url_for('lessons') }}" class="mr-4 text-blue-500 hover:underline">Lessons</a>
            <a href="{{ url_for('profile') }}" class="mr-4 text-blue-500 hover:underline">Profile</a>
            <a href="{{ url_for('phonics_game') }}" class="mr-4 text-blue-500 hover:underline">Phonics Game</a>
            {% if user_id %}
            <a href="{{ url_for('logout') }}" class="text-red-500 hover:underline">Logout</a>
            {% endif %}
        </nav>
    </header>
    <main class="container mx-auto p-4">
        {% with messages = get_flashed_messages(with_categories=true) %}
            {% if messages %}
                {% for category, message in messages %}
                    <div class="mb-4 p-4 rounded {{ 'bg-green-100 text-green-700' if category == 'success' else 'bg-red-100 text-red-700' }}">
                        {{ message }}
                    </div>
                {% endfor %}
            {% endif %}
        {% endwith %}
        {% block content %}{% endblock %}
    </main>
    <footer class="bg-white dark:bg-gray-700 p-4 mt-6 text-center text-gray-500 dark:text-gray-400">
        <p>&copy; 2025 EduGrok. All rights reserved.</p>
    </footer>
    {% block scripts %}{% endblock %}
</body>
</html>