<!DOCTYPE html>
<html lang="{{ language }}">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>EduGrok - {{ title | default('Home') }}</title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
    <link rel="stylesheet" href="{{ url_for('static', filename='styles.css') }}">
</head>
<body class="theme-{{ theme | default('astronaut') }}">
    <nav class="nav-bar">
        <div class="container mx-auto flex justify-between items-center">
            <h1 class="text-xl font-bold">EduGrok - Dino Cowboy Adventures!</h1>
            {% if session.user_id %}
                <div class="flex space-x-4">
                    <a href="{{ url_for('home') }}" class="hover:underline">Home</a>
                    <a href="{{ url_for('lessons') }}" class="hover:underline">Lessons</a>
                    <a href="{{ url_for('phonics_game') }}" class="hover:underline">Phonics Game</a>
                    <a href="{{ url_for('profile') }}" class="hover:underline">Profile</a>
                    <a href="{{ url_for('logout') }}" class="hover:underline">Logout</a>
                </div>
            {% else %}
                <div class="flex space-x-4">
                    <a href="{{ url_for('login') }}" class="hover:underline">Login</a>
                    <a href="{{ url_for('register') }}" class="hover:underline">Register</a>
                </div>
            {% endif %}
        </div>
    </nav>

    <main class="container mx-auto p-4">
        {% with messages = get_flashed_messages(with_categories=true) %}
            {% if messages %}
                {% for category, message in messages %}
                    <div class="bg-{{ 'green' if category == 'success' else 'red' }}-100 p-4 rounded mb-4 border-l-4 border-{{ 'green' if category == 'success' else 'red' }}-500">
                        {{ message }}
                    </div>
                {% endfor %}
            {% endif %}
        {% endwith %}

        {% block content %}{% endblock %}
    </main>

    <footer class="bg-gray-800 text-white p-4 mt-8 text-center">
        <p>&copy; 2025 EduGrok - Built with Grok by xAI. Pirate vibes all the way! 🏴‍☠️</p>
    </footer>
</body>
</html>