<!DOCTYPE html>
<html lang="{{ language }}">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ 'EduGrok: Fun Learning for Kids' if language == 'en' else 'EduGrok: Lekker Leer vir Kinders' }}</title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
</head>
<body class="{% if theme == 'astronaut' %}bg-blue-900 text-white{% elif theme == 'farm' %}bg-lime-200 text-black{% else %}bg-blue-900 text-white{% endif %} min-h-screen flex flex-col">
    <nav class="bg-blue-700 text-white p-4 shadow-md flex justify-between items-center sticky top-0 z-50">
        <div class="container mx-auto flex justify-between items-center">
            <h1 class="text-xl font-bold">EduGrok - Dino Cowboy Adventures!</h1>
            <div class="flex space-x-4">
                <a href="/login" class="hover:underline">{{ 'Login' if language == 'en' else 'Teken in' }}</a>
                <a href="/register" class="hover:underline">{{ 'Sign Up' if language == 'en' else 'Registreer' }}</a>
            </div>
        </div>
    </nav>
    <main class="flex-1 container mx-auto p-6 max-w-4xl text-center">
        <section class="mb-8">
            <h2 class="text-4xl font-bold mb-6 text-gray-800 dark:text-white">{{ 'Fun, Free Learning for Grades 1-3!' if language == 'en' else 'Lekker, Gratis Leer vir Graad 1-3!' }}</h2>
            <p class="text-xl mb-8 text-gray-700 dark:text-gray-300 leading-relaxed">{{ 'CAPS-aligned lessons in Math, Language, and Science with interactive games.' if language == 'en' else 'CAPS-gebaseerde lesse in Wiskunde, Taal en Wetenskap met interaktiewe speletjies.' }}</p>
            <a href="/register" class="inline-block bg-green-500 text-white px-8 py-4 rounded-xl text-xl font-semibold hover:bg-green-600 transition-all duration-200 shadow-lg hover:shadow-xl transform hover:-translate-y-1">{{ 'Get Started Free!' if language == 'en' else 'Begin Gratis!' }}</a>
        </section>
        <section class="bg-white dark:bg-gray-800 p-6 rounded-xl shadow-lg border border-gray-200 dark:border-gray-700 mb-8">
            <h3 class="text-2xl font-semibold mb-6 text-gray-800 dark:text-white">{{ 'Features' if language == 'en' else 'Kenmerke' }}</h3>
            <ul class="text-left space-y-4 list-disc pl-6 text-gray-700 dark:text-gray-300">
                <li class="text-lg leading-relaxed">{{ 'Interactive phonics and handwriting practice' if language == 'en' else 'Interaktiewe fonika en handskrif-oefening' }}</li>
                <li class="text-lg leading-relaxed">{{ 'Bilingual (English/Afrikaans) support' if language == 'en' else 'Tweetalige (Engels/Afrikaans) ondersteuning' }}</li>
                <li class="text-lg leading-relaxed">{{ 'Progress tracking for parents' if language == 'en' else 'Vorderingsnasioming vir ouers' }}</li>
                <li class="text-lg leading-relaxed">{{ 'Fun themes: Farm, Space, Astronaut' if language == 'en' else 'Vrolike temas: Plaas, Ruimte, Ruimtevaarder' }}</li>
            </ul>
        </section>
    </main>
    <footer class="bg-gray-800 text-white p-4 mt-auto text-center">
        <p>&copy; 2025 EduGrok - Built with Grok by xAI. Pirate vibes all the way! 🏴‍☠️</p>
    </footer>
</body>
</html>